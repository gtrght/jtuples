    /**
     * Zips arrays into single array of tuples
     * @return
     */
    @SuppressWarnings("unchecked")
    public static <[TN]> Tuple[N]<[TN]>[] zip([ATN]) [[
        int resultSize = _max([ALN]);
        Tuple[N][] result = new Tuple[N][resultSize];

        for (int i = 0; i < resultSize; i++)
            result[i] = tuple([NAI]);

        return result;
    ]]